# [Module/feature name]: Identify the main module, function, or feature that was modified, and update to the [] below.
# Example: auth, api_handler, db_migration, cache, user_profile, etc.
# AI Hint: Analyze git diff to determine the primary module, feature, or function modified.
# AI Hint: Generate a concise commit message title with a verb that describes the change.
[Module/feature name] Commit Message Title

[Description]
# Describe the changes made in more detail.
# AI Hint: Summarize the key changes by analyzing the diff. Use clear, concise language.
# - What functionality was added, modified, or fixed?
# - Highlight any changes that may impact other modules.
# - Mention if dependencies, APIs, or configs were modified.
# - Use bullet points or a short paragraph for better readability.
# Example:
# - Refactored login flow for faster response time.
# - Added caching to API responses to reduce DB load.
# - Fixed race condition in user session handling.

[Motivation]
# Explain why this change is needed.
# AI Hint: Identify the purpose behind the change.
# - Was it a bug fix, performance improvement, or feature addition?
# - If related to a ticket/issue, mention the reference.
# - State the problem that the change addresses.
# Example:
# - Resolves issue #123 where users could not log in after session timeout.
# - Improves query performance by adding indexes.

[Self-Test]
# Describe the tests performed before committing.
# AI Hint: List any tests or checks performed to validate the changes.
# - Unit tests? Integration tests? Manual verification?
# - Mention if the build or CI passed successfully.
# - If no tests were conducted, add "No tests performed."
# Example:
# - Build succeeded.
# - Unit tests passed with coverage > 90%.
# - Manually verified API response with Postman.
# - No tests performed (for documentation updates).

# Example Commit message.
# [auth] Add OAuth2 Token Refresh Mechanism
#
# [Description]
# - Implemented automatic token refresh for OAuth2 API clients.
# - Updated auth middleware to check token expiry and refresh if needed.
# - Added error handling for token renewal failures.
#
# [Motivation]
# - Resolves issue #345 where users experienced session timeouts.
# - Improves user experience by maintaining authenticated sessions.
#
# [Self-Test]
# - Build succeeded.
# - Manual testing with Postman for token renewal workflow.
# - Unit tests for token refresh logic passed.

# Note. # is for explination, don't output it.
